# Template for Python Experiments 

This template provide a ready to use poetry configuration and GitHub workflows to automatically create a docker image and release it on Dockerhub. 

## Requirements

In order for the workflows to correctly run you must set two repository secretes:
1. `DOCKER_USERNAME`
2. `DOCKER_PASSWORD`

